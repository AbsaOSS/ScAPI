{
  "endpoint" : "getUserCurrent",
  "tests": [
    {
      "name" : "get current user",
      "categories": ["SMOKE"],
      "headers" : [
        {
          "name": "authorization",
          "value": "{{ constants.header_bearer_token }}"
        },
        {
          "name": "content-type",
          "value": "{{ constants.content_type }}"
        }
      ],
      "actions": [
        {
          "methodName": "get",
          "url": "{{ env.url }}/GUI/user"
        }
      ],
      "assertions": [
        {
          "group": "assert",
          "name": "status-code",
          "param_1": "200"
        }
      ]
    }
  ]
}
