{
  "endpoint" : "getUserCurrent",
  "tests": [
    {
      "name" : "get current user",
      "categories": ["SMOKE"],
      "headers" : [
        {
          "name": "authorization",
          "value": "{{ constants.header_bearer_token }}"
        },
        {
          "name": "contentType",
          "value": "{{ constants.content_type }}"
        },
        {
          "name": "accept",
          "value": "true"
        }
      ],
      "actions": [
        {
          "name": "get",
          "value": "{{ env.url }}/GUI/user"
        }
      ],
      "assertions": [
        {
          "name": "statusCode",
          "value": "200"
        }
      ]
    }
  ]
}
