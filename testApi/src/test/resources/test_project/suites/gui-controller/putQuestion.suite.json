{
  "endpoint" : "putQuestion",
  "tests": [
    {
      "name" : "test-name-1",
      "categories": ["SMOKE"],
      "headers" : [
        {
          "name": "authorization",
          "value": "{{ constants.header_auth }}"
        },
        {
          "name": "content-type",
          "value": "{{ constants.content_type }}"
        }
      ],
      "actions": [
        {
          "methodName": "put",
          "url": "{{ env.url }}/path/question"
        }
      ],
      "assertions": [
        {
          "name": "status-code",
          "value": "200"
        }
      ]
    }
  ]
}
