{
  "endpoint" : "putQuestion",
  "tests": [
    {
      "name" : "test-name-1",
      "categories": ["SMOKE"],
      "headers" : [
        {
          "name": "authorization",
          "value": "{{ env.basic_token }}"
        },
        {
          "name": "content-type",
          "value": "{{ constants.content_type }}"
        }
      ],
      "actions": [
        {
          "methodName": "put",
          "url": "{{ env.url }}/path/question"
        }
      ],
      "assertions": [
        {
          "group": "assert",
          "name": "status-code",
          "param_1": "200"
        }
      ]
    }
  ]
}
